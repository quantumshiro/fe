
#[test]
fn test_multiple_effect_params() uses (x: mut u256, y: mut u256) {
    x += 1
    y += 2
    assert(true)
}

#[test]
fn test_non_evm_effect_param() uses (value: mut u256) {
    value += 1
    assert(true)
}
