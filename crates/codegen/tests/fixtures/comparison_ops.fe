pub fn comparison_ops() -> (bool, bool, bool, bool, bool, bool) {
    // TODO: 1u256 syntax
    let one: u256 = 1
    let two: u256 = 2
    let three: u256 = 3
    (one == one, one != two, one < two, two <= two, three > two, three >= three)
}
