// Writing through a parent place while a loan to a projected child is live should be rejected.

struct S {
    a: u256,
    b: u256,
}

pub fn borrowck_write_struct_under_field_mut() {
    let mut s: S = S { a: 0, b: 1 }
    let p: mut u256 = mut s.a
    s = S { a: 2, b: 3 }
    p = 4
}
