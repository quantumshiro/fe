// Multiple recv blocks for the same msg type are not allowed

msg BankMsg {
    Deposit,
}

pub contract BadContract {
    data: u64,

    recv BankMsg {
        Deposit {}
    }

    recv BankMsg {
        Deposit {}
    }
}
