trait Bound {
    type Repr
}

struct Unknown {}

impl Bound for Unknown {
    type Repr = usize
}

struct Wrapper<T: Bound> {
    pub item: <T as Bound>::Repr,
}

fn test_unknown(n: usize) -> Wrapper<Unknown> {
    Wrapper { item: n }
}

