trait Processor {
    fn process(self) -> i32
}

struct Widget {
    id: i32
}

impl Processor for Widget {
    fn process(self) -> i32 {
        self.id
    }
}

fn use_generic<P: Processor>(p: P) -> i32 {
    p.process()
}

fn use_concrete(w: Widget) -> i32 {
    w.process()
}
