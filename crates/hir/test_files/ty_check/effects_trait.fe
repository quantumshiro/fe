pub trait Logger {
    fn log(mut self)
}

pub struct Console {}
impl Logger for Console {
    fn log(mut self) {}
}

pub fn log_once()
  uses (logger: mut Logger)
{
    logger.log()
}

pub fn provide_logger() {
    with (Console {}) {
        log_once()
    }
}
