pub struct Storage {
    value: u64,
}

pub fn mint(_ amount: u64)
  uses (mut st: Storage)
{
    st.value += amount
}

pub fn example() {
    let mut st = Storage { value: 0 }
    with (Storage = st) {
        mint(5)
    }
}
