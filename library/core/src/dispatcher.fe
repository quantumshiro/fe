/// Minimal interface that Fe contracts need to implement so the generated
/// runtime can delegate control to user code.
pub trait Dispatcher {
    /// Entry point invoked from the generated Yul runtime during every call.
    ///
    /// The function does not return a value. Implementations should handle
    /// decoding calldata, invoking user logic, and returning or reverting as
    /// needed using the available intrinsics.
    fn dispatch()
}
